# 使用说明

## 快速开始

### 1. 安装依赖

```bash
npm install
```

首次安装会下载以下依赖：
- express - Web服务器
- multer - 文件上传处理
- canvas - 图片绘制
- sharp - 图片处理
- tesseract.js - OCR文字识别

### 2. 启动服务

```bash
npm start
```

服务器将在 `http://localhost:3000` 启动。

### 3. 打开Web界面

在浏览器中访问 `http://localhost:3000`

## 详细操作步骤

### 上传截图

1. 点击或拖拽上传朋友圈截图
2. 支持 JPG、PNG 格式
3. 建议使用高清截图（分辨率越高，识别越准确）

### 配置选项

#### 1. 智能布局检测（推荐）✨
- **默认开启**
- 自动识别时间位置，适配不同尺寸
- 首次运行会下载OCR语言包（约5-10MB）
- 增加1-3秒处理时间，但准确度更高

**何时关闭：**
- 处理速度要求极高
- 截图时间文字模糊不清
- 使用固定模板截图

#### 2. 截图类型
- **自动识别**：根据图片比例自动判断
- **详情页**：单条朋友圈的详情页面
- **时间线**：朋友圈列表流

**判断方法：**
- 详情页：顶部显示"详情"，底部有评论输入框
- 时间线：顶部显示"朋友圈"，包含多条动态

#### 3. 显示时间
- **随机时间**：从预设列表中随机选择
  - 刚刚、1分钟前、3分钟前、5分钟前...
- **自定义时间**：输入任意文本
  - 例如：10分钟前、昨天 18:30、2026年1月14日

#### 4. 点赞人数
- 留空：随机生成 10-30 人
- 输入数字：生成指定数量的点赞
- 范围：0-100

#### 5. 评论数量
- 留空：随机生成 5-12 条评论
- 输入数字：生成指定数量的评论
- 范围：0-50

#### 6. 自定义点赞名单（可选）
- 用逗号分隔名字
- 例如：`张三,李四,王五,赵六`
- 填写后会忽略"点赞人数"设置

### 生成截图

1. 配置完成后点击"生成截图"按钮
2. 等待处理（智能检测约3-5秒，默认布局约1秒）
3. 自动下载修改后的图片
4. 可以在预览区查看效果

## 两种截图样式对比

### 详情页截图示例

```
┌─────────────────────┐
│      详情    ⋮      │  顶部标题
├─────────────────────┤
│                     │
│  头像  用户名       │
│  新年紫金山         │
│                     │
│  [图片网格]         │
│  [图片网格]         │
│                     │
│  2026年1月2日 19:17 │  ← 时间位置
│                     │
│  ❤ 张三,李四,王五   │  ← 点赞区域
│                     │
│  张三: 哈哈哈哈     │  ← 评论区域
│  李四: 太棒了       │
│                     │
├─────────────────────┤
│  评论...     📷 😊  │  底部输入框
└─────────────────────┘
```

### 时间线截图示例

```
┌─────────────────────┐
│    朋友圈       📷   │  顶部标题
├─────────────────────┤
│                     │
│ 头像  用户A         │
│       一条朋友圈... │
│       12天前        │
│                     │
├─────────────────────┤
│ 头像  用户B         │  ← 要修改的这条
│       新年紫金山    │
│       [图片]        │
│       12天前        │  ← 时间位置
│       ❤ 点赞列表    │  ← 点赞区域
│       评论...       │  ← 评论区域
├─────────────────────┤
│ 头像  用户C         │
│       另一条...     │
│       12天前        │
└─────────────────────┘
```

## 智能检测原理

### OCR识别流程

1. **扫描区域选择**
   - 只扫描图片中下部分（30%-80%区域）
   - 跳过顶部和底部，提高识别速度

2. **时间文字匹配**
   - 识别所有文字
   - 匹配时间模式：
     - X秒前、X分钟前、X小时前、X天前
     - 刚刚
     - 2026年1月14日 19:17
     - 1月14日 19:17

3. **坐标计算**
   ```
   时间位置 (x, y)
      ↓
   点赞区域 = (x - 偏移, y + 60-70px)
      ↓
   评论区域 = (x - 偏移, y + 125-140px)
   ```

4. **字体大小适配**
   - 根据识别到的时间字体高度
   - 自动调整其他元素的字体大小
   - 保持视觉一致性

### 降级策略

```
开始
  ↓
启用智能检测？
  ↓ No ──────────→ 使用默认布局
  ↓ Yes
执行OCR识别
  ↓
识别到时间？
  ↓ No ──────────→ 使用默认布局
  ↓ Yes
计算智能布局
  ↓
绘制图片
```

## 性能优化建议

### 1. 提高识别准确度
- 使用高分辨率截图（推荐1080p以上）
- 确保时间文字清晰可见
- 避免截图模糊或压缩过度

### 2. 提高处理速度
- 关闭智能检测（处理速度提升2-3倍）
- 手动指定截图类型（跳过自动识别）
- 使用较小的图片尺寸

### 3. 批量处理
```javascript
// 使用批量API
const formData = new FormData();
formData.append('screenshots', file1);
formData.append('screenshots', file2);
formData.append('screenshots', file3);

fetch('/api/batch-modify', {
  method: 'POST',
  body: formData
});
```

## 常见使用场景

### 场景1：集赞活动
**需求：** 修改朋友圈时间为"刚刚"，添加30个点赞

**操作：**
1. 上传截图
2. 时间选择：自定义 → "刚刚"
3. 点赞人数：30
4. 评论数量：0（不添加评论）
5. 生成截图

### 场景2：纪念日修改
**需求：** 将一年前的朋友圈改成今天发布的

**操作：**
1. 上传截图
2. 时间选择：自定义 → "2026年1月14日 12:00"
3. 保持其他默认设置
4. 生成截图

### 场景3：添加互动效果
**需求：** 给朋友圈添加真实感的点赞和评论

**操作：**
1. 上传截图
2. 开启智能检测
3. 点赞人数：25
4. 评论数量：8
5. 自定义点赞名单：填入真实好友名字
6. 生成截图

## 故障排除

### 问题1：OCR语言包下载失败
**症状：** 首次运行卡在"正在处理图片"

**解决：**
1. 检查网络连接
2. 手动下载语言包：https://github.com/naptha/tessdata
3. 放置到 `node_modules/tesseract.js-core/` 目录

### 问题2：位置不准确
**症状：** 时间、点赞、评论位置偏移

**解决：**
1. 启用智能检测
2. 手动指定截图类型
3. 检查截图是否清晰
4. 如果仍不准确，可能需要调整代码中的偏移量

### 问题3：内存不足
**症状：** 处理大图片时崩溃

**解决：**
1. 压缩图片尺寸后再上传
2. 增加 Node.js 内存限制：
   ```bash
   node --max-old-space-size=4096 src/server.js
   ```
3. 关闭智能检测以减少内存使用

### 问题4：中文识别不准
**症状：** OCR无法识别时间文字

**解决：**
1. 确保截图清晰
2. 检查 OCR 语言包是否正确安装
3. 考虑关闭智能检测，使用默认布局

## 进阶技巧

### 技巧1：保存常用配置
可以在代码中设置默认值：

```javascript
// src/editor.js
this.defaultLikesRange = { min: 20, max: 35 };  // 修改默认点赞范围
this.defaultCommentsRange = { min: 8, max: 15 }; // 修改默认评论范围
```

### 技巧2：自定义随机名字库
```javascript
// src/editor.js - generateRandomNames()
const names = [
  // 添加你自己的名字库
  '张三', '李四', '王五', ...
];
```

### 技巧3：调整布局偏移
```javascript
// src/layoutDetector.js - calculateLayoutFromTime()
likes: {
  y: timePosition.baseline + 70,  // 调整点赞距离时间的间距
},
comments: {
  startY: timePosition.baseline + 150, // 调整评论距离时间的间距
}
```

## 开发模式

启动开发服务器（自动重启）：

```bash
npm run dev
```

## 技术支持

- 查看详细技术文档：[AUTO_DETECT_SOLUTION.md](AUTO_DETECT_SOLUTION.md)
- 了解完整功能：[README.md](README.md)
- GitHub Issues：提交问题和建议
